# Frontend Future Work

This folder is reserved for the future React + Vite + TypeScript frontend.

Planned boundaries:
- App shell and routing live here.
- Chat UI, source panels, and settings screens will live here later.
- This folder must not import Python tooling directly.
- API access should go through a typed HTTP client once the backend exists.

No framework scaffold is created yet by design.