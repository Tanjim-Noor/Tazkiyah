# Quran Data Collection Script - Dependencies
# Install with: pip install -r requirements.txt

# HTTP requests with connection pooling and retry support
# Used for all API calls to Quran Foundation API
requests

# Fast, extensible progress bar for Python
# Used for nested progress bars (chapters → verses)
tqdm

# Python CLI toolkit for building command-line interfaces
# Used for argument parsing and user interaction
click
